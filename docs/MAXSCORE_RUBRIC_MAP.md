# JurisTwin Sentinel v5.7 — Level-5 Rubric Evidence Map

This is a judge-preparation document, not a claim that human scoring is guaranteed. It identifies the strongest live proof for each Level-5 descriptor.

## Core Functionality & Feature Execution — 30%

**Track-2-first proof:** the first screen asks a plain-language question, checks five governed sources, shows `CONFLICT_PRESENT`, cites the source mix and can switch to an Intern-redacted view before any architecture tour.

**Level-5 proof to show:**

- judge types or drops completely unseen policy evidence;
- hybrid AI analyses it at runtime;
- conflicting evidence is quarantined, never silently made canonical;
- BFS traces the real affected cohort;
- all three seeded policy domains have distinct end-to-end workflows;
- HMAC-signed external HTTP ingress works from another process;
- Secure Enterprise Memory answers plain-language questions with evidence and role controls.

**Anti-hardcoding proof:** switch from income policy to restructuring or notification and execute its distinct Twin → gate → decision → replay → Proof Pack.

## Technical Depth & Architecture — 25%

**Level-5 proof to show:**

- real local learned NLP pipeline with measured held-out development metrics;
- symbolic Policy Atom Reasoner with modality/numeric/temporal collision rules;
- confidence gating and abstention;
- Authority-Weighted Hybrid Consensus;
- graph BFS blast-radius traversal;
- 1,500-scenario Monte Carlo + sensitivity + Pareto robustness;
- enforced Governance Gate;
- proof-carrying Decision Assurance dossier;
- SHA-256 linked ledger + HMAC verification;
- JWT/RBAC/DLP, replay protection, transactions, invariants, telemetry and rate containment.

## User Experience & Interface Usability — 20%

**Level-5 proof to show:**

- five-screen information architecture;
- large projector-safe hierarchy + Presentation Mode;
- native scrolling + sticky chrome;
- responsive bounded draggable graph;
- progressive disclosure rather than card overload;
- consistent side-sheet close paths;
- aria-live status, landmark structure, accessible names, reduced-motion support;
- **Ask JurisTwin is first-class and above the fold**;
- plain-language verified-answer interaction with multi-source synthesis + citations;
- one-click Manager → Intern role proof on the same question.

## Technical Innovation & Creativity — 15%

**Strongest differentiators:**

1. **Proof-carrying governed decisions** — model boundary, evidence, conflict reasoning, impact, simulation, human governance and ledger state are signed together.
2. **No-silent-truth mutation** — untrusted evidence can be analysed automatically but cannot become canonical without authority and human governance.
3. **Hybrid learned + symbolic + authority consensus** — statistical generalisation is deliberately separated from policy authority.
4. **Decision Digital Twin + Bodyguard loop** — detect → trace → simulate → approve → propagate → protect → replay.
5. **Judge-controlled live evidence** — the product can be challenged with unseen content rather than relying on a prepared scenario.

## System Stability & Error Handling — 10%

**Release evidence:**

- 57/57 automated tests;
- 32/32 championship preflight controls;
- 16/16 Attack Sentinel controls;
- structured 401/403/404/422 handling;
- global error containment;
- rollback-safe transactions;
- post-approval readiness remains 100%;
- no static runtime secret literals;
- automatic local port failover;
- deterministic reset;
- external AI/internet not required for core demo;
- fresh-process HTTP stress: **60/60 concurrent live writes succeeded**, ledger chain remained valid, readiness stayed 100%.

## One-line positioning

> **JurisTwin is the decision-integrity layer that makes enterprise AI answers trustworthy when the organisation itself disagrees.**
