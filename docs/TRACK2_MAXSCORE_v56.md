# JurisTwin Sentinel v5.6 — Track 2 MaxScore Layer

v5.6 preserves the complete v5.5/v4 governance stack but changes the judge-facing hierarchy so the Track 2 requirement is impossible to miss.

## First 90 seconds
1. **Ask JurisTwin** is the first navigation destination.
2. The prepared question is already above the fold: `Can gig workers use bank statements as income evidence?`
3. A manager receives an evidence-bound answer with **multi-source synthesis** across governed Outlook, Teams, FSD, training and customer evidence.
4. One click switches to **Intern** and proves server-side redaction on the same question.
5. `CONFLICT_PRESENT` is shown rather than flattening incompatible evidence into a fake answer.
6. The judge can then inject unseen evidence in Evidence Lab and continue the pitch flow.

## Track 2 proof
- Aggregation: governed evidence layer across mail/chat/doc/customer sources.
- Natural language: `/api/memory/answer`, visible on Overview.
- Access control: role-aware retrieval and server-side redaction.
- Multi-source summary: `source_mix` + `synthesis`, entirely evidence-bound.
- Process optimisation: Option C is explicitly labelled process optimisation and connects FSD, training, officer communication, QA and duplicate-request controls.

## Runtime truthfulness
- Finals semantic retrieval is **local BM25 + cosine**, not ChromaDB.
- ChromaDB remains explicitly labelled a **pilot-target architecture** from the pitch deck.
- Microsoft-branded integrations are deterministic finals adapters unless a real tenant is connected.
- Genuine machine-to-machine live ingress is demonstrated through the HMAC-SHA256 signed webhook.

## Remaining championship stack
Hybrid learned NLP, symbolic Policy Atom Reasoner, authority-weighted consensus, BFS blast radius, 1,500-scenario Monte Carlo, sensitivity/Pareto analysis, Governance Gate, progressive rollout, Decision Replay, AI Bodyguard, Decision Ledger, HMAC Proof Pack, proof verification, Attack Sentinel, RBAC/DLP, telemetry and operational invariants remain intact.
