# JurisTwin Sentinel — Pitch-Aligned Championship v5.3

## UX objective

v5.3 keeps the JurisTech-aligned large-type, native-scroll application shell from v5.2 while aligning every live workflow to the final 12-slide pitch deck. The judge-facing navigation stays intentionally small: **Overview, Conflict Map, Digital Twin, Assurance, Evidence Lab**. Deeper capabilities are exposed contextually through **Platform** and **Final Flow**, so technical depth does not become visual clutter.

## Pitch-aligned interaction model

The primary operating loop is now identical in the website and pitch deck:

**CONNECT → EXPOSE → SIMULATE → RECOMMEND → APPROVE → PROTECT**

The Final Flow panel shows the same operating impact used in the deck: **27 applications · 1 rejected case · 8 QA tests · 3 documents superseded · 4 officers notified**. The approved JT-084 decision contract separately records **2 directly affected policy documents**, while propagation supersedes a third downstream training document.

## Preserved championship features

The simplified UI still exposes Secure Enterprise Memory, role-aware retrieval, Policy Atom reasoning, the draggable conflict graph, BFS blast-radius proof, the 1,500-scenario White-Box Future Simulator, sensitivity/Pareto robustness, the enforced Governance Gate, AI Bodyguard, Decision Ledger, progressive rollout, Decision Replay, signed Decision Assurance Proof Pack, Attack Sentinel, HMAC external ingress, telemetry, invariants and release provenance.

## Browser-validation boundary

The underlying responsive shell, native document scrolling, drawer close paths and graph drag/bounds mechanics were browser-validated in v5.2 at 1024×768, 1440×900 and 1920×1080. v5.3 preserves those mechanics and adds pitch-aligned content/actions. A fresh Playwright run cannot be honestly claimed in this packaging environment because managed Chromium blocks all local URLs through an administrator URL blocklist. Therefore v5.3 is validated with JavaScript syntax checks, static UI contract tests, API/E2E regression tests, clean Uvicorn HTTP smoke tests and the inherited browser-tested shell. Run one final visual rehearsal on the actual finals laptop after extraction.
