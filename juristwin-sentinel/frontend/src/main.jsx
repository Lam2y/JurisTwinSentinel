import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{api}from'./api/client';import'./style.css';
function App(){const[token,setToken]=useState(localStorage.getItem('jt_token'));const[page,setPage]=useState('dashboard');const[data,setData]=useState(null);async function login(){const d=await api('/auth/login',{method:'POST',body:JSON.stringify({email:'operations@regulatedbank.com',password:'Finals2026!'})});localStorage.setItem('jt_token',d.access_token);setToken(d.access_token)}useEffect(()=>{if(!token)return;const p=page==='dashboard'?'/dashboard':page==='case'?'/cases/JT-2026-084':page==='conflict'?'/conflicts/CF-INCOME-001':page==='twin'?'/simulations/conflict/CF-INCOME-001':page==='ledger'?'/ledger/decisions':'/integrations';api(p).then(setData)},[token,page]);if(!token)return <main className="login"><h1>JurisTwin Sentinel</h1><p>React client connected to the live finals backend.</p><button onClick={login}>Enter Finalist Demo</button></main>;return <div className="shell"><aside><h2>JurisTwin<br/><small>SENTINEL</small></h2>{['dashboard','case','conflict','twin','ledger','integrations'].map(x=><button onClick={()=>setPage(x)} key={x}>{x}</button>)}</aside><main><h1>{page}</h1><pre>{JSON.stringify(data,null,2)}</pre><p>This lightweight React shell proves frontend-backend connectivity. Use <b>/finals</b> for the polished no-build presentation UI.</p></main></div>}
createRoot(document.getElementById('root')).render(<App/>);
