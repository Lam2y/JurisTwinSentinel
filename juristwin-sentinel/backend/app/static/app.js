const S={token:localStorage.getItem('jt_token'),user:null,page:'dashboard',guide:false,sim:null,approval:null};
const nav=[['dashboard','Command Center'],['case','Case Workspace'],['conflict','Conflict Intelligence'],['twin','Digital Twin'],['memory','Enterprise Memory'],['ledger','Decision Ledger'],['bodyguard','AI Bodyguard'],['integrations','Integrations'],['admin','Administration']];
function esc(x){return String(x??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));}
async function api(path,opt={}){const h={'Content-Type':'application/json',...(opt.headers||{})};if(S.token)h.Authorization=`Bearer ${S.token}`;const r=await fetch('/api'+path,{...opt,headers:h});if(!r.ok){const t=await r.text();throw new Error(t)}return r.json();}
function toast(m){const e=document.getElementById('toast');e.textContent=m;e.style.display='block';setTimeout(()=>e.style.display='none',3200)}
async function login(){try{const d=await api('/auth/login',{method:'POST',body:JSON.stringify({email:email.value,password:password.value})});S.token=d.access_token;S.user=d.user;localStorage.setItem('jt_token',S.token);showApp();render();}catch(e){loginError.textContent='Login failed. Use the finalist demo credentials.'}}
function demoLogin(){email.value='operations@regulatedbank.com';password.value='Finals2026!';login()}
async function showApp(){document.getElementById('login').classList.add('hidden');document.getElementById('app').classList.remove('hidden');if(!S.user)S.user=await api('/auth/me');userName.textContent=S.user.name;userRole.textContent=S.user.role.replaceAll('_',' ');renderNav()}
function renderNav(){navEl=document.getElementById('nav');navEl.innerHTML=nav.map(([k,n])=>`<button class="${S.page===k?'active':''}" onclick="go('${k}')">${n}</button>`).join('')}
function go(p){S.page=p;renderNav();render()}
function setHead(c,t){crumb.textContent=c;pageTitle.textContent=t}
async function render(){try{const c=document.getElementById('content');c.innerHTML='<div class="muted">Loading governed state…</div>';if(S.page==='dashboard')await dashboard();if(S.page==='case')await casePage();if(S.page==='conflict')await conflictPage();if(S.page==='twin')await twinPage();if(S.page==='memory')await memoryPage();if(S.page==='ledger')await ledgerPage();if(S.page==='bodyguard')await bodyguardPage();if(S.page==='integrations')await integrationsPage();if(S.page==='approval')await approvalPage();if(S.page==='admin')await adminPage();}catch(e){content.innerHTML=`<div class="card critical"><h3>Request failed</h3><pre>${esc(e.message)}</pre></div>`}}
function guideBox(step){return S.guide?`<div class="guide"><b>Guided Demo — Step ${step}</b><div class="muted">${['','Start with the company-wide signal.','Show the customer journey and contradictory evidence.','Trace authority and exact conflict lineage.','Test three futures before changing reality.','Human approval turns advice into a governed decision.','Verify the append-only chain.','Trigger and restore a protected decision.'][step]||''}</div></div>`:''}
function toggleGuide(){S.guide=!S.guide;render()}
async function dashboard(){setHead('OPERATIONS / DASHBOARD','Sentinel Command Center');const d=await api('/dashboard');content.innerHTML=guideBox(1)+`<div><h1>${esc(d.greeting)}</h1><div class="muted">${esc(d.summary)}</div></div><div class="grid cols5">${[['Active Cases',d.metrics.active_cases,''],['Decision Conflicts',d.metrics.decision_conflicts,'critical'],['Customers at Risk',d.metrics.customers_at_risk,'warning'],['Protected Decisions',d.metrics.protected_decisions,'good'],['Security Alerts',d.metrics.security_alerts,'critical']].map(x=>`<div class="card metric ${x[2]}"><label>${x[0]}</label><strong>${x[1]}</strong></div>`).join('')}</div><div class="grid cols2"><div class="card"><h3>Priority Decision Conflicts</h3><div class="list">${d.priority_conflicts.map(x=>`<div class="row"><div><b>${esc(x.name)}</b><div class="meta">${x.affected_customers} customers at risk · ${x.systems_affected} systems</div></div><span class="badge ${x.severity==='Critical'?'red':x.severity==='High'?'amber':''}">${x.severity}</span></div>`).join('')}</div><button class="danger" onclick="go('case')" style="margin-top:14px">Investigate Income-Document Conflict</button></div><div class="card"><h3>Decision Integrity Score</h3><div style="font-size:48px;font-weight:800">${d.integrity.score}</div><div class="muted">${d.integrity.threshold}</div>${[['Evidence Alignment',d.integrity.evidence_alignment],['Version Consistency',d.integrity.version_consistency],['Access Compliance',d.integrity.access_compliance],['Decision Propagation',d.integrity.decision_propagation]].map(x=>`<div style="margin-top:14px"><div class="kv"><span>${x[0]}</span><b>${x[1]}%</b></div><div class="progress"><span style="width:${x[1]}%"></span></div></div>`).join('')}</div></div>`}
async function casePage(){setHead('WORKSPACE / CUSTOMER FILE','Customer Case Workspace');const d=await api('/cases/JT-2026-084');const x=d.case;content.innerHTML=guideBox(2)+`<div class="card"><div class="row"><div><h2 style="margin:0">${esc(x.customer_name)} <span class="muted">(${esc(x.customer_type)})</span></h2><div class="meta">Case ${x.case_ref} · ${x.application_type}</div></div><div class="actions"><span class="badge red">${x.risk_status} Risk</span><span class="badge amber">${x.sentiment} Sentiment</span><span class="badge">${x.pending_days} days pending</span></div></div></div><div class="grid cols3"><div class="card"><h3>Documents Verified</h3><div class="list">${d.evidence.filter(e=>['FSD','Outlook Approval','Customer Core System'].includes(e.source)).map(e=>`<div class="row"><div>${esc(e.title)}</div><span class="badge ${e.approved?'green':e.superseded?'red':'amber'}">${e.approved?'Verified':e.superseded?'Outdated':'Unresolved'}</span></div>`).join('')}</div></div><div class="card"><h3>Integrated Live Case Timeline</h3><div class="timeline">${d.timeline.map(e=>`<div class="event ${e.severity}"><b>${esc(e.title)}</b><div class="muted">${esc(e.description)}</div></div>`).join('')}</div></div><div class="card critical"><h3>Twin Core State Conflict</h3><p><span class="badge green">Approved Instruction</span><br>Bank statements satisfy requirement.</p><p><span class="badge amber">Operational Instruction</span><br>Staff demanding payslips via email.</p><p><span class="badge red">Observed System Behavior</span><br>${esc(x.current_blocker||'Case aligned after governed resolution.')}</p><button onclick="go('conflict')">Open Conflict Network Graph</button></div></div>`}
async function conflictPage(){setHead('INTELLIGENCE / INTERACTIVE GRAPH','Evidence Network Graph');const d=await api('/conflicts/CF-INCOME-001');content.innerHTML=guideBox(3)+`<div class="grid cols2"><div class="card graph"><h3>Evidence Topology</h3>${d.graph.nodes.map(n=>`<div class="node ${n.color}"><b>${esc(n.label)}</b><div class="meta">${esc(n.source||n.type)} ${n.authority?'· '+esc(n.authority):''} ${n.version?'· '+esc(n.version):''}</div>${n.claim?`<div>${esc(n.claim)}</div>`:''}</div>`).join('')}</div><div class="card"><h3>Conflict Explanation</h3><label class="muted">ROOT CAUSE</label><p>${esc(d.root_cause)}</p><label class="muted">RESOLUTION RECOMMENDATION</label><p style="color:var(--green)">${esc(d.recommendation)}</p><label class="muted">SYSTEM CONFIDENCE</label><p><span class="badge green">High Confidence</span> ${(d.confidence*100).toFixed(1)}%</p><p class="muted">Linked Sources: ${d.graph.nodes.length-1}</p><button onclick="go('twin')">Simulate Resolution Options</button></div></div>`}
async function twinPage(){setHead('INTELLIGENCE / SIMULATOR','Decision Digital Twin');S.sim=await api('/simulations/conflict/CF-INCOME-001');const opts=S.sim.options;content.innerHTML=guideBox(4)+`<div><h1>Decision Digital Twin</h1><div class="muted">Test possible interventions before changing the live process.</div></div><div class="grid cols3">${opts.map(o=>`<div class="card option ${o.key===S.sim.recommended_option?'recommended':''}"><div class="kv"><h3>OPTION ${o.key}</h3>${o.key===S.sim.recommended_option?'<span class="badge green">AI RECOMMENDED</span>':''}</div><h3>${esc(o.name)}</h3><div class="kv"><span>Predicted Delay</span><b>${o.predicted_delay_days} days</b></div><div class="kv"><span>Complaint Probability</span><b>${o.complaint_probability}%</b></div><div class="kv"><span>Applications Affected</span><b>${o.applications_affected}</b></div><div class="kv"><span>Duplicate Requests</span><b>${o.duplicate_requests}</b></div><div class="kv"><span>Policy Alignment</span><b>${o.policy_alignment}%</b></div><div class="kv"><span>Decision Fit</span><b>${o.decision_fit}%</b></div></div>`).join('')}</div><div class="grid cols2"><div class="card"><h3>Adjust Weights</h3><div class="weights grid cols3"><label>Delay<input id="wDelay" type="number" step="0.05" value="${S.sim.weights.delay}"></label><label>Complaint<input id="wComplaint" type="number" step="0.05" value="${S.sim.weights.complaint}"></label><label>Alignment<input id="wAlignment" type="number" step="0.05" value="${S.sim.weights.alignment}"></label></div><button class="ghost" onclick="rerunSim()" style="margin-top:12px">Re-run White-Box Simulation</button></div><div class="card"><h3>Why Option ${S.sim.recommended_option}?</h3><p class="muted">Complete alignment updates the FSD, training materials, QA tests and human execution path at the same time. Every factor remains inspectable.</p><div class="kv"><span>Simulation Confidence</span><b>${(S.sim.confidence*100).toFixed(0)}%</b></div><div class="kv"><span>Evidence Count</span><b>7 Linked Sources</b></div><div class="kv"><span>Similar Match Cases</span><b>38 Precedents</b></div><button class="goodbtn" onclick="submitApproval()">Submit Scenario for Approval</button></div></div>`}
async function rerunSim(){S.sim=await api('/simulations/conflict/CF-INCOME-001/run',{method:'POST',body:JSON.stringify({weights:{delay:+wDelay.value,complaint:+wComplaint.value,alignment:+wAlignment.value}})});toast(`Simulation ${S.sim.sim_ref} completed — Option ${S.sim.recommended_option} recommended`);render()}
async function submitApproval(){if(!S.sim)S.sim=await api('/simulations/conflict/CF-INCOME-001');S.approval=await api(`/approvals/simulation/${S.sim.sim_ref}/submit`,{method:'POST',body:JSON.stringify({selected_option:S.sim.recommended_option,comments:'Submitted from finalist demo'})});S.page='approval';renderNav();render();toast('Decision proposal created and routed for human approval.')}
async function memoryPage(){setHead('SEARCH / ENTERPRISE MEMORY','Secure Enterprise Memory');content.innerHTML=`<div><h1>Secure Enterprise Memory</h1><div class="muted">Connect by meaning. Reveal by permission.</div></div><div class="card"><div class="searchbox"><input id="memq" value="bank statements payslips income rule" placeholder="Search customers, policies, decisions, messages or evidence…"><button onclick="doMemorySearch()">Search</button></div><div id="memResults" class="list" style="margin-top:16px"></div></div>`;doMemorySearch()}
async function doMemorySearch(){const q=document.getElementById('memq')?.value||'';const d=await api('/memory/search',{method:'POST',body:JSON.stringify({query:q,limit:10})});const e=document.getElementById('memResults');if(e)e.innerHTML=d.results.map(x=>`<div class="row"><div><b>${esc(x.title)}</b><div class="meta">${esc(x.source)} · ${esc(x.sensitivity)} · ${Math.round((x.score||0)*100)}% semantic match</div><div>${esc(x.body)}</div></div><span class="badge ${x.approved?'green':x.superseded?'red':''}">${x.approved?'Approved':x.status}</span></div>`).join('')}
async function ledgerPage(){setHead('SECURITY & COMPLIANCE','Decision Ledger');const ds=await api('/ledger/decisions');const v=await api('/ledger/verify');content.innerHTML=guideBox(6)+`<div class="grid cols2"><div class="card"><h3>Decision Contracts</h3>${ds.length?ds.map(d=>`<div class="row"><div><b>${d.decision_ref}</b><div>${esc(d.approved_rule)}</div><div class="meta">${esc(d.version)} · ${esc(d.approved_by)}</div></div><span class="badge green">${d.status}</span></div>`).join(''):'<div class="muted">No governed contract yet. Approve Option C first.</div>'}</div><div class="card ${v.ok?'good':'critical'}"><h3>Immutable Ledger Audit Trail</h3><div class="kv"><span>Integrity</span><b>${v.ok?'VERIFIED':'FAILED'}</b></div><div class="kv"><span>Entries</span><b>${v.entries}</b></div><div class="kv"><span>Errors</span><b>${v.errors.length}</b></div><p class="muted">Head hash: ${esc((v.head_hash||'GENESIS').slice(0,32))}…</p><div class="actions"><button class="ghost" onclick="verifyLedger()">Verify Complete SHA-256 Chain</button><button class="ghost" onclick="exportLedger()">Export Audit Report</button></div></div></div>`}
async function verifyLedger(){const v=await api('/ledger/verify');toast(v.ok?`Ledger verified: ${v.entries} entries, 0 errors`:`Ledger verification failed`)}
async function bodyguardPage(){setHead('SECURITY / AI BODYGUARD','AI Bodyguard Security Center');const alerts=await api('/bodyguard/alerts');const a=alerts[0];content.innerHTML=guideBox(7)+`<div><h1>AI Bodyguard Security Center</h1><div class="muted">Protecting approved decisions, sensitive evidence and customer data.</div></div>${a?`<div class="grid cols2"><div class="card critical"><h3>ALERT: ${esc(a.title)}</h3><div class="kv"><span>Triggering User</span><b>${esc(a.user_ref)}</b></div><div class="kv"><span>Document</span><b>${esc(a.document)}</b></div><div class="kv"><span>Action</span><b>${esc(a.action)}</b></div><div class="kv"><span>Risk Status</span><b>${esc(a.severity)}</b></div><div class="list">${a.timeline.map(t=>`<div class="row"><span>${t.action}</span><span class="badge ${t.status==='completed'?'green':t.status==='alerted'?'red':''}">${t.time||t.status}</span></div>`).join('')}</div></div><div class="card"><h3>Why was this flagged?</h3><div class="list">${a.reasons.map(r=>`<div class="row">${esc(r)}</div>`).join('')}</div>${a.status==='open'?`<button class="goodbtn" onclick="restoreAlert('${a.alert_ref}')" style="margin-top:14px">Restore Approved Version (Recommended)</button>`:'<div class="badge green" style="margin-top:14px">Approved version restored. Incident recorded in Decision Ledger.</div>'}</div></div>`:`<div class="card"><h3>No active incident</h3><p class="muted">After publishing JT-084, trigger a safe demo attack to prove post-decision protection.</p><button class="danger" onclick="simulateAttack()">Simulate Approved-Decision Modification</button></div>`}`}
async function simulateAttack(){const d=await api('/bodyguard/simulate-attack',{method:'POST'});toast(`${d.title}: ${d.user_ref} → ${d.document}`);render()}
async function restoreAlert(ref){await api(`/bodyguard/alerts/${ref}/restore`,{method:'POST'});toast('Approved version restored and cryptographically logged.');render()}
async function integrationsPage(){setHead('INTEGRATIONS','Enterprise Core Integrations');const d=await api('/integrations');content.innerHTML=`<div><h1>Enterprise Core Integrations</h1><div class="muted">Manage digital twin ingestion pipelines and active connectors.</div></div><div class="grid cols3">${d.map(i=>`<div class="card"><div class="kv"><h3>${esc(i.name)}</h3><span class="badge green">${esc(i.status)}</span></div><div class="kv"><span>Indexed objects</span><b>${i.object_count.toLocaleString()}</b></div><div class="kv"><span>Shield</span><b>${esc(i.shield_status)}</b></div><button class="ghost" onclick="syncInt('${i.key}')">Sync Now</button></div>`).join('')}</div>`}
async function syncInt(k){const d=await api(`/integrations/${k}/sync`,{method:'POST'});toast(`${d.name} synced. ${d.object_count.toLocaleString()} objects indexed.`);render()}
async function approvalPage(){setHead('WORKFLOWS / CASE ACTION','Approve Recommended Resolution');const approvals=await api('/approvals');const a=S.approval||approvals.find(x=>x.status==='pending')||approvals[0];const status=await api('/demo/status');content.innerHTML=guideBox(5)+`<div class="grid cols2"><div class="card"><h3>Proposed Decision Rule Amendment</h3><p><b>Gig workers may submit verified bank statements as acceptable income evidence.</b></p><div class="kv"><span>Conflict Decision</span><b>CF-INCOME-001</b></div><div class="kv"><span>Recommended Scenario</span><b>Option C — Align Complete Process</b></div><div class="kv"><span>Affected Enterprise Assets</span><b>27 apps · 8 QA tests · 3 tasks · 2 docs</b></div><h3>Recommended Action Steps</h3><div class="list">${['Update Financial Specification Document (FSD v4.0)','Update active operations training materials & FAQs','Dispatch automated notifications to active processing officers','Re-evaluate 27 active applications currently marked incomplete','Reopen 5 previously rejected cases for fast-track reconciliation'].map(x=>`<div class="row">${x}</div>`).join('')}</div></div><div class="card"><h3>Authorization Process</h3>${a?`<div class="kv"><span>Approval Ref</span><b>${a.approval_ref}</b></div><div class="kv"><span>Status</span><b>${a.status}</b></div><div class="kv"><span>Requested By</span><b>${a.requested_by}</b></div>${a.status==='pending'?`<textarea id="approvalComments" rows="4" placeholder="Add optional review feedback or compliance notes..."></textarea><button class="goodbtn" onclick="approve('${a.approval_ref}')" style="margin-top:12px">Approve & Publish Resolution</button>`:`<div class="badge green">Decision JT-084 approved and published.</div><button onclick="go('ledger')" style="margin-top:12px">Open Decision Ledger</button>`}`:`<div class="muted">Run the simulation and submit Option C first.</div><button onclick="go('twin')">Open Digital Twin</button>`}<hr style="border-color:var(--line);margin:20px 0"><div class="kv"><span>Conflict Status</span><b>${status.conflict_status}</b></div><div class="kv"><span>Decision Published</span><b>${status.decision_published?'YES':'NO'}</b></div></div></div>`}
async function adminPage(){setHead('ADMINISTRATION / SYSTEM SETUP','Integrations & Administration');const cfg=await api('/system/config');const health=await api('/system/health');content.innerHTML=`<div><h1>Administration & System Setup</h1><div class="muted">Set operational access baselines, active shielding and ledger retention controls.</div></div><div class="grid cols2"><div class="card"><h3>Role-Aware Decrypted Memory Matrix</h3><table><tr><th>Role</th><th>Access</th><th>Sensitivity</th><th>Override</th></tr>${cfg.rbac.map(r=>`<tr><td>${esc(r.role)}</td><td>${esc(r.access)}</td><td>${esc(r.sensitivity)}</td><td>${r.override?'Yes':'No'}</td></tr>`).join('')}</table></div><div class="card"><h3>Active Security Shield Parameters</h3><div class="list">${cfg.shields.map(s=>`<div class="row"><div><b>${esc(s.name)}</b><div class="meta">${esc(s.rule)}</div></div><span class="badge green">${esc(s.status)}</span></div>`).join('')}</div><div class="kv"><span>Retention</span><b>${esc(cfg.retention)}</b></div><div class="kv"><span>Ledger Integrity</span><b>${health.decision_ledger.ok?'VERIFIED':'FAILED'}</b></div><button class="ghost" onclick="resetDemo()">Reset Finalist Demo State</button></div></div>`}
async function approve(ref){const d=await api(`/approvals/${ref}/approve`,{method:'POST',body:JSON.stringify({comments:document.getElementById('approvalComments')?.value||'Approved in finalist demo'})});toast(`${d.decision_contract.decision_ref} approved and published across ${d.propagation.cases} cases.`);S.approval=d.approval;render()}
async function resetDemo(){if(!confirm('Reset the deterministic Grand Finals dataset?'))return;await api('/demo/reset',{method:'POST'});S.sim=null;S.approval=null;toast('Grand Finals dataset reset.');S.page='dashboard';renderNav();render()}
(async()=>{if(S.token){try{S.user=await api('/auth/me');showApp();render()}catch(e){localStorage.removeItem('jt_token');S.token=null}}})();

async function exportLedger(){const r=await fetch('/api/ledger/export.csv',{headers:{Authorization:`Bearer ${S.token}`}});if(!r.ok){toast('Audit export failed');return}const b=await r.blob();const u=URL.createObjectURL(b);const a=document.createElement('a');a.href=u;a.download='juristwin_decision_ledger.csv';a.click();URL.revokeObjectURL(u);toast('Audit report exported.')}
